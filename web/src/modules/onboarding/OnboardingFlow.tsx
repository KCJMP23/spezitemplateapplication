import { Box, Typography } from '@mui/material';

export default function OnboardingFlow(): JSX.Element {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Onboarding Flow</Typography>
      <Typography>Welcome! Complete your onboarding to continue.</Typography>
    </Box>
  );
}
